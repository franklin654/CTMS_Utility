package com.ctms.ctms_backend.milestone.entity;

/** First Patient In / Last Patient In / Last Patient Out / Database Lock -- CLAUDE.md's canonical
 * abbreviation set, don't invent alternates. */
public enum MilestoneType {
    FPI,
    LPI,
    LPO,
    DBL
}
